import type { MunicipioSummary as Summary } from "../../types/device";

export function MunicipioSummary({ rows, expanded, onToggle, onSelect }: { rows: Summary[]; expanded: boolean; onToggle: () => void; onSelect: (municipio: string) => void }) {
  const visible = expanded ? rows : rows.slice(0, 7);
  return <section className="rounded-2xl border border-zinc-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><div><h3 className="font-black">Resumo por município</h3><p className="mt-1 text-xs text-zinc-500">Distribuição dos ativos monitorados.</p></div><button onClick={onToggle} className="text-xs font-black text-blue-600">{expanded ? "Mostrar menos" : "Ver todos os municípios"}</button></div><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[520px] text-sm"><thead><tr className="border-b text-left text-xs uppercase tracking-wide text-zinc-400"><th className="pb-3">Município</th><th className="pb-3 text-center">Online</th><th className="pb-3 text-center">Atenção</th><th className="pb-3 text-center">Offline</th><th className="pb-3 text-right">Total</th></tr></thead><tbody>{visible.map((row) => <tr key={row.municipio} onClick={() => onSelect(row.municipio)} className="cursor-pointer border-b border-zinc-100 transition last:border-0 hover:bg-blue-50"><td className="py-3 font-black">{row.municipio}</td><td className="py-3 text-center font-bold text-emerald-600">{row.online}</td><td className="py-3 text-center font-bold text-amber-600">{row.warning}</td><td className="py-3 text-center font-bold text-red-600">{row.offline}</td><td className="py-3 text-right font-black">{row.total}</td></tr>)}</tbody></table></div></section>;
}
