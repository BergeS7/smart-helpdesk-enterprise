import { AlertTriangle, ChevronRight, Monitor, Wifi, WifiOff } from "lucide-react";
import type { Device } from "../../types/device";
export function MunicipioDevices({ municipio, devices, onSelect, onClear }: { municipio: string; devices: Device[]; onSelect: (device: Device) => void; onClear: () => void }) {
  const online = devices.filter((item) => item.status === "online").length;
  return <aside className="rounded-2xl border border-zinc-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] font-black uppercase tracking-widest text-blue-600">Visão do município</p><h3 className="mt-1 text-xl font-black">{municipio}</h3></div><button onClick={onClear} className="text-xs font-black text-blue-600">Ver estado</button></div><div className="mt-4 grid grid-cols-3 gap-2"><div className="rounded-xl bg-zinc-50 p-3"><Monitor size={15}/><b className="mt-2 block text-lg">{devices.length}</b><span className="text-[10px] text-zinc-500">Ativos</span></div><div className="rounded-xl bg-emerald-50 p-3 text-emerald-700"><Wifi size={15}/><b className="mt-2 block text-lg">{online}</b><span className="text-[10px]">Online</span></div><div className="rounded-xl bg-red-50 p-3 text-red-700"><AlertTriangle size={15}/><b className="mt-2 block text-lg">{devices.length-online}</b><span className="text-[10px]">Alertas</span></div></div><p className="mt-5 text-xs font-black text-zinc-500">COMPUTADORES DA UNIDADE</p><div className="mt-2 max-h-[430px] space-y-2 overflow-y-auto pr-1">{devices.map((device) => <button key={device.id} onClick={() => onSelect(device)} className="flex w-full items-center gap-3 rounded-xl border border-zinc-100 p-3 text-left hover:border-blue-200 hover:bg-blue-50"><span className={`grid h-9 w-9 place-items-center rounded-xl ${device.status === "online" ? "bg-emerald-50 text-emerald-600" : "bg-red-50 text-red-600"}`}>{device.status === "online" ? <Wifi size={16}/> : <WifiOff size={16}/>}</span><span className="min-w-0 flex-1"><b className="block truncate text-sm">{device.hostname}</b><span className="block truncate text-[11px] text-zinc-500">{device.usuario} · {device.ip}</span></span><ChevronRight size={15} className="text-zinc-300"/></button>)}</div></aside>;
}
